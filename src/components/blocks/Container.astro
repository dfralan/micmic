---
interface Props {
        customClass?: string
        id?: string
}

const { customClass, id } = Astro.props;
const defaultClass = "max-w-7xl mx-auto px-6 md:px-12 xl:px-6 py-6 md:py-12"
---

<div id={id} class:list={[ defaultClass, customClass ]} >
    <slot />
</div>